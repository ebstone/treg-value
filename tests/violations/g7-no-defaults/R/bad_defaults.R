# Violation fixture for G7 (no defaults): supplies a default for an open
# item's argument (OPEN_QUESTIONS.md O1 -> eligible_population_fractions).

compute_breakeven <- function(eligible_population_fractions = 0.2) {
  eligible_population_fractions
}
